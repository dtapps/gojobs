// 版本
syntax = "proto3";

// 包名
package pb;

// 别名
option go_package = "../pb";

// 定义服务
service PubSub {
  // [发布] 消息
  rpc Publish (String) returns (String);
  // [订阅] 消息
  rpc Subscribe (String) returns (stream String);
}

// 消息
message String {
  string value = 1;
}
